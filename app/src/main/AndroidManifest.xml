<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.d_bug.chungbukapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/img_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".GreetingActivity" />
        <activity android:name=".SummaryActivity" />
        <activity android:name=".SymbolActivity" />
        <activity android:name=".OrganizingActivity" />
        <activity android:name=".DirectionActivity" />
        <activity android:name=".MyWebView" />
        <activity android:name=".ParaHomeActivity" />
        <activity android:name=".ParaGreetingActivity" />
        <activity android:name=".ParaSummaryActivity" />
        <activity android:name=".ParaSymbolActivity"></activity>
        <receiver android:name=".AlarmReceive"></receiver>
    </application>

</manifest>